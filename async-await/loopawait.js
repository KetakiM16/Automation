let fs = require("fs");
let files = [ "f1.txt","f2/txt"]




async function loop(filepath){
    for(i = 0; i< files.length;i++){
    filepath = await Promise.all([page.click(Selector),page.waitForNavigation()])
    filepath = files[i];

    }
}